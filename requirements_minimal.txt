# Minimal requirements for NO₂ download only
# Install with: pip install -r requirements_minimal.txt

# Google Earth Engine (REQUIRED for NO₂ download)
earthengine-api>=0.1.380

# Data handling
numpy>=1.21.0
pandas>=1.3.0

# Utilities
python-dotenv>=0.19.0
tqdm>=4.64.0

# Note: Install full requirements.txt later for ML training
